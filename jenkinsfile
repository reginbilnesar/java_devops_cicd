@Library('my-shared-library') _
pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                script {
                    call(branch: 'main', url: 'https://github.com/reginbilnesar/java_devops_cicd.git')
                }
            }
        }

        // Additional stages...
    }
}

